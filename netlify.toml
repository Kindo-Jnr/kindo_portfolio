[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "build"  # or "build" for create-react-app; adjust to your project setup

[build.environment]
  NODE_VERSION = "18.20.8"
